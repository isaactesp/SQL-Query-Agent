# SQL-Query-Agent
This repo aims to delve into dbt -which stands for data building tool- for data modeling, working on the three layer data architecture. Furthermore, an LLM will be connected, so the users can ask concrete data from the gold layer using natural language.   
